package com.teamaurora.magnetosphere.impl.registry.forge;

import com.teamaurora.magnetosphere.impl.registry.DeferredRegisterImpl;
import net.minecraft.core.Registry;
import net.minecraft.resources.ResourceKey;

public class DeferredRegisterImplImpl {
    public static DeferredRegisterImpl<T> create(ResourceKey<? extends Registry<T>> registryKey, String modId) {
    }
}
